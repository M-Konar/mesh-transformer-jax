{
  "layers": 28,
  "d_model": 4096,
  "n_heads": 16,
  "n_vocab": 50400,
  "norm": "layernorm",
  "pe": "rotary",
  "pe_rotary_dims": 64,

  "seq": 2048,
  "cores_per_replica": 8,
  "per_replica_batch": 1,
  "gradient_accumulation_steps": 16,

  "warmup_steps": 100,
  "anneal_steps": 900,
  "lr": 5e-5,
  "end_lr": 1e-5,
  "weight_decay": 0.1,
  "total_steps": 1000,

  "tpu_size": 8,

  "bucket": "apg-gptj",
  "model_dir": "finetuned_models/model1",

  "train_set": "APG.train.index",
  "val_set": {
    
  },

  "eval_harness_tasks": [
  ],

  "val_batches": 1704,
  "val_every": 10000,
  "ckpt_every": 300,
  "keep_every": 300,

  "name": "GPT3_6B_APG_V1.1",
  "comment": ""
}